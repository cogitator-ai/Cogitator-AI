apiVersion: v1
kind: Service
metadata:
  name: {{ include "cogitator.fullname" . }}
  labels:
    {{- include "cogitator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cogitator.selectorLabels" . | nindent 4 }}
